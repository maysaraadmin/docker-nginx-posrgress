# Simple test page to verify HTTP access
server {
    listen 8888;
    server_name _;
    
    location / {
        return 200 "HTTP Server Working! Time: $time_local\n";
        add_header Content-Type text/plain;
    }
}
